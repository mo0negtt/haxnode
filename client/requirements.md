## Packages
@xyflow/react | Visual node editor core library
monaco-editor | Code editor for previewing generated scripts
@monaco-editor/react | React wrapper for Monaco editor
file-saver | For downloading the exported script
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes without conflicts

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
